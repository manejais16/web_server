#TODO:remove remote port bind
#Volume for mariaDB
services:
  mariadb:
    image: mariadb_main
    hostname: mariadb
    container_name: mariadb_inc
    volumes:
      - mariadb_vol:/mariadb
      - ${HOME}/data/secrets/db_password.txt:/mariadb/secrets/db_password.txt
      - ${HOME}/data/secrets/db_root_password.txt:/mariadb/secrets/db_root_password.txt
    ports:
      - '3306:3306'
    networks:
      - wordpress 
    build:
      dockerfile: ./requirements/mariadb/Dockerfile
#  wordpress:
#    image: wordpress_main
#    hostname: wordpress
#    container_name: wordpress_inc
#    volumes:
#      - wordpress_vol:/wordpress
#      - ${HOME}/data/secrets/wordpress_password.txt:/wordpress/secrets/wordpress_password.txt
#      - ${HOME}/data/secrets/db_password.txt:/mariadb/secrets/db_password.txt
#    port:
#      - '9000':'9000'
#    networks:
#      - wordpress
#      - nginx
#    build:
#      dockerfile: ./requirements/wordpress/Dockerfile

#Volume for WordPress+PHP
#volumes:
#  - type: bind
#    source: ./requirements/wordpress
#    target: /wordpress
#
#Volume for NGINX
#volumes:
#  - type: bind
#    source: ./requirements/wordpress
#    target: /www


networks:
  wordpress:
    driver: bridge
  nginx:
    driver: bridge

volumes:
  mariadb_vol:
    driver: local
    driver_opts:
      type: none
      device: ${HOME}/data/srcs/requirements/mariadb
      o: bind
  wordpress_vol:
    driver: local
    driver_opts:
      type: none
      device: ${HOME}/data/srcs/requirements/wordpress
      o: bind
